# UNCONF CLI configuration

# Base API endpoint used by the CLI client.
api_endpoint: http://localhost:8080

# Log level for CLI output (debug, info, warn, error).
log_level: info

# Optional explicit config file path override.
config_file: ./.unconf.yaml
