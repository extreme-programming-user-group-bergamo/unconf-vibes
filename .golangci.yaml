version: "2"

run:
  timeout: 5m

output:
  formats:
    text:
      path: stdout
      colors: true

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - errcheck
    - unused
    - staticcheck
  disable:
    - revive

formatters:
  enable:
    - gofmt
    - goimports