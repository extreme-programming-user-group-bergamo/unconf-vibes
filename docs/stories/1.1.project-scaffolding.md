# Story 1.1: Project Scaffolding

**Status:** Ready for Done

**Epic:** 1 - Foundation & Authentication

---

## Story

**As a** developer,  
**I want** the project repository initialized with Go module, folder structure, and basic tooling,  
**so that** I have a clean foundation to build upon.

---

## Acceptance Criteria

1. Go module initialized (`go.mod`) with appropriate module path
2. Standard Go project layout created (`/cmd`, `/internal`, `/pkg`)
3. `.gitignore` configured for Go projects
4. `README.md` with project overview and setup instructions
5. Makefile or task runner with common commands (`build`, `test`, `lint`)
6. `golangci-lint` configuration file present

---

## Dev Notes

### Technology Stack Reference

[Source: architecture/tech-stack.md#core-technologies]

- **Language:** Go 1.24+ (primary requirement for all application code)
- **Versions:**
  - Go: 1.24+ (newer performance and tooling baseline per architecture decision)
  - SQLite: 3.40+ (already decided; includes JSON functions, window functions)
  - golangci-lint: v2.x (pin latest stable)

### Project Structure & File Locations

[Source: architecture/11-unified-project-structure.md]

The repository structure MUST follow this layout:

```
unconf/
├── .github/
│   └── workflows/
│       ├── ci.yaml              # Test, lint on PR
│       ├── release.yaml         # GoReleaser on tag
│       └── deploy.yaml          # Deploy to Fly.io
├── cmd/
│   ├── unconf/                  # CLI binary entry point
│   │   └── main.go
│   └── server/                  # API server binary entry point
│       └── main.go
├── internal/
│   ├── api/                     # HTTP API (Gin handlers)
│   ├── cli/                     # CLI commands (Cobra)
│   ├── tui/                     # TUI components (Bubble Tea)
│   ├── service/                 # Business logic layer
│   ├── repository/              # Data access layer
│   │   ├── interfaces.go
│   │   └── sqlite/
│   ├── models/                  # Domain models/structs
│   ├── auth/                    # Authentication logic
│   ├── email/                   # Email service
│   ├── config/                  # Configuration management
│   └── client/                  # HTTP client for CLI
├── migrations/                  # Database migrations (golang-migrate format)
├── configs/                     # Configuration files (.yaml, .env examples)
├── scripts/                     # Build/deploy scripts
├── docs/                        # Documentation
├── Dockerfile
├── docker-compose.yml
├── fly.toml
├── Makefile
├── .goreleaser.yaml
├── .golangci.yaml
├── go.mod
├── go.sum
└── README.md
```

### Coding Standards

[Source: architecture/coding-standards.md#naming-conventions]

- **Files:** snake_case (e.g., `booking_service.go`, `booking_test.go`)
- **Packages:** lowercase, single word (e.g., `service`, `repository`, `api`)
- **Go modules naming convention:** Use descriptive package names following Go conventions

### Testing Strategy

[Source: architecture/15-testing-strategy.md]

- **Go 1.24+** includes built-in testing via `go test`
- **Test pyramid:** 60% unit, 30% integration, 10% E2E
- **Testing dependencies:** stretchr/testify v1.11+ for assertions (will be added to go.mod in later story)

### Required Go Module Path

No specific guidance in architecture docs. Standard Go convention: Use GitHub repository path as module name.  
Suggested: `github.com/katurdays/unconf` (based on workspace path structure)

---

## Tasks / Subtasks

### Task 1: Initialize Go Module (AC: 1)

Create `go.mod` file with appropriate module path and initial dependencies.

**Subtasks:**
- [x] Run `go mod init github.com/katurdays/unconf` to initialize module
- [x] Verify `go.mod` file created at repository root
- [x] Set Go version directive to `go 1.24` in `go.mod`
- [x] Commit `go.mod` file

**Testing:** Verify with `go list -m all` returns the module

---

### Task 2: Create Standard Go Project Layout (AC: 2)

Create the directory structure following [Source: architecture/11-unified-project-structure.md].

**Subtasks:**
- [x] Create `/cmd/unconf/` directory with placeholder `main.go` (prints "UNCONF CLI")
- [x] Create `/cmd/server/` directory with placeholder `main.go` (prints "UNCONF API Server")
- [x] Create `/internal/` with subdirectories:  
   - `api/` (placeholder file to prevent empty dir)
   - `cli/` (placeholder file)
   - `tui/` (placeholder file)
   - `service/` (placeholder file)
   - `repository/` with `interfaces.go` (empty file) and `sqlite/` subdirectory
   - `models/` (placeholder file)
   - `auth/` (placeholder file)
   - `email/` (placeholder file)
   - `config/` (placeholder file)
   - `client/` (placeholder file)
- [x] Create `/migrations/` directory (for golang-migrate)
- [x] Create `/configs/` directory
- [x] Create `/scripts/` directory
- [x] Create `/docs/` directory

**Testing:** Verify directory structure matches architecture spec: `find . -type d -name "internal" -o -name "cmd" | xargs find`

---

### Task 3: Configure `.gitignore` for Go (AC: 3)

Create `.gitignore` file with Go-specific patterns.

**Subtasks:**
- [x] Create `.gitignore` at repository root
- [x] Add Go-specific patterns:
   - `*.o`, `*.a`, `*.so`
   - `*.test`
   - `*.out`
   - `/bin/`
   - `/dist/`
   - `.DS_Store` (macOS)
   - `.env`, `.env.local`
   - `*.db`, `*.sqlite` (SQLite databases)
   - `config.local.yaml` (local overrides)
   - `debug` executables
- [x] Commit `.gitignore`

**Testing:** Verify with `git check-ignore -v *.db go.test` returns matches

---

### Task 4: Create `README.md` with Project Overview (AC: 4)

Create comprehensive README with setup instructions for developers.

**Subtasks:**
- [x] Create `README.md` at repository root
- [x] Include sections:
   - **Project Overview:** Brief description of UNCONF CLI project
   - **Quick Start:** Installation and basic usage (`unconf login`, `unconf help`)
   - **Development Setup:** Prerequisites (Go 1.24+, SQLite 3.40+, Make, golangci-lint)
   - **Building:** `make build` instructions
   - **Testing:** `make test` instructions
   - **Linting:** `make lint` instructions
   - **Project Structure:** Reference to `docs/architecture/11-unified-project-structure.md`
   - **Documentation:** Links to `docs/architecture/`, `docs/prd/`
   - **Contributing:** Link to coding standards at `docs/architecture/coding-standards.md`
- [x] Reference tech stack versions from [Source: architecture/tech-stack.md]

**Testing:** Verify README is readable and all links are valid: `grep -E "^\[|^#" README.md`

---

### Task 5: Create Makefile with Build Commands (AC: 5)

Create `Makefile` with essential development tasks.

**Subtasks:**
- [x] Create `Makefile` at repository root
- [x] Define targets:
   - `make build` — Compile CLI and API binaries to `/bin/unconf` and `/bin/unconf-server`
   - `make test` — Run `go test ./...` with `-race` flag
   - `make lint` — Run `golangci-lint run ./...`
   - `make run-cli` — Build and run CLI binary
   - `make run-server` — Build and run API server binary
   - `make clean` — Remove `/bin/` and test artifacts
- [x] Add `.PHONY` declarations for all targets
- [x] Include phony targets for future: `migrate-up`, `migrate-down`, `docker-build`

**Testing:** Verify targets exist: `make -n build lint test` shows commands without executing

---

### Task 6: Create `golangci-lint` Configuration (AC: 6)

Create `.golangci.yaml` configuration file for linting standards.

**Subtasks:**
- [x] Create `.golangci.yaml` at repository root
- [x] Configure linters:
   - Enable: `gofmt`, `goimports`, `vet`, `ineffassign`, `errcheck`, `unused`, `staticcheck`, `gosimple`
   - Disable: `revive` (too many false positives)
- [x] Set timeout to `5m`
- [x] Exclude patterns for test files (if needed)
- [x] Set output format to `colored-line-number`

**Testing:** Verify with `golangci-lint version && golangci-lint run --no-config ./... --debug` returns linter info

---

### Task 7: Verify All Files Created & Commit

Verify all acceptance criteria files are in place and commit foundation.

**Subtasks:**
- [x] Verify files exist:
   - `go.mod` ✓
   - Directory structure per AC2 ✓
   - `.gitignore` ✓
   - `README.md` ✓
   - `Makefile` ✓
   - `.golangci.yaml` ✓
- [x] Run `make lint` — Should pass (or have only suppressed issues)
- [x] Run `go mod tidy` to clean up `go.mod` if needed
- [ ] Commit all files with message: "Story 1.1: Project Scaffolding Foundation"

**Testing:** Final verification:
```bash
git status  # Should show clean working tree
make build  # Should produce /bin/unconf and /bin/unconf-server
make test   # Should pass (no tests yet, but command should work)
make lint   # Should pass
```

---

## Testing

### Unit Tests

No unit tests needed for scaffolding story. Placeholder test file can be created at `internal/service/service_test.go` with a single passing test to verify test framework works.

### Integration Tests

Can wait until database story (1.4). No integration needed for scaffolding.

### Manual Verification

- [x] Repository structure matches architecture spec
- [x] All Makefile targets execute successfully (`build`, `test`, `lint`)
- [x] `go test ./...` runs without errors (even if no tests exist)
- [x] `golangci-lint run ./...` passes
- [x] README is clear and directs to architecture docs

---

## Project Structure Notes

The unified project structure defined in [architecture/11-unified-project-structure.md] is comprehensive and already matches Go best practices. No conflicts noted between epic requirements and architecture specifications.

---

## Change Log

| Date | Author | Change |
|------|--------|--------|
| 2026-02-14 | Scrum Master | Draft created from Epic 1.1 with full architecture context |
| 2026-02-14 | James (Dev) | Implemented project scaffolding foundation, verified build/test/lint, and updated story record |

---

## Dev Agent Record

*(To be filled by developer during implementation)*

### Completion Notes

- [ ] All tasks completed
- [x] All acceptance criteria verified
- [x] Makefile tested: `build`, `test`, `lint` working
- [ ] Project ready for next story (1.2: CLI Framework Setup)

### Implementation Deviations

- `gosimple` is not available as a standalone linter in the installed `golangci-lint` build; `staticcheck` and enabled formatters cover equivalent baseline quality checks.
- Story asks for committing changes; commit was intentionally not performed in this implementation session.

### Debug Log references

- None.

### Lessons Learned

- For `golangci-lint` v2, formatters (`gofmt`, `goimports`) must be configured under `formatters`, not `linters`.

### Agent Model Used

- GPT-5.3-Codex

### File List

- go.mod
- Makefile
- .golangci.yaml
- .gitignore
- README.md
- cmd/unconf/main.go
- cmd/server/main.go
- internal/repository/interfaces.go
- internal/api/.gitkeep
- internal/cli/.gitkeep
- internal/config/.gitkeep
- internal/repository/sqlite/.gitkeep

## QA Results

### Review Date: 2026-02-14

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation is clean, minimal, and aligned with the intended scaffolding scope. Build, test, and lint commands execute successfully. The only notable gap is Acceptance Criterion 6 mismatch: story asks for `gosimple` explicitly, while `.golangci.yaml` does not enable it.

### Refactoring Performed

None.

### Compliance Check

- Coding Standards: ✓ Uses `log/slog` in entry points, package/file naming and structure are consistent.
- Project Structure: ✓ Required Go layout and scaffolding directories are present (`cmd`, `internal`, `pkg`, plus supporting dirs).
- Testing Strategy: ✓ `make test` executes successfully for current scaffold stage; no functional test scope required yet.
- All ACs Met: ✗ AC6 is partially unmet due to missing explicit `gosimple` enablement in linter config.

### Requirements Traceability (Given-When-Then)

- AC1 (Go module): Given repository root, when reading `go.mod`, then module path and `go 1.24` are present.
- AC2 (Project layout): Given repository tree, when inspecting `cmd/`, `internal/`, `pkg/`, then standard layout exists.
- AC3 (`.gitignore`): Given ignore config, when reviewing patterns, then Go/macOS/env/sqlite artifacts are covered.
- AC4 (`README.md`): Given onboarding docs, when reading `README.md`, then overview and setup/build/test/lint instructions exist.
- AC5 (Makefile): Given task runner, when executing `make build && make test && make lint`, then commands run successfully.
- AC6 (`golangci-lint` config): Given `.golangci.yaml`, when checking enabled linters, then `gosimple` is not explicitly enabled (concern).

### Improvements Checklist

- [x] Verified build/test/lint execution evidence
- [x] Validated AC1-AC5 against repository artifacts
- [ ] Add explicit `gosimple` enablement (or update AC/story to reflect v2 linter behavior)
- [ ] Add a minimal smoke test file in next story to improve baseline observability

### Security Review

No security-impacting code paths were introduced in this scaffolding story. No secrets appear committed in reviewed files.

### Performance Considerations

No runtime performance concerns identified at this stage; scope is project bootstrapping and tooling.

### Files Modified During Review

- docs/stories/1.1.project-scaffolding.md

### Gate Status

Gate: CONCERNS → docs/qa/gates/1.1-project-scaffolding.yml
Risk profile: not generated in this review
NFR assessment: not generated in this review

### Recommended Status

[✗ Changes Required - See unchecked items above]
(Story owner decides final status)
