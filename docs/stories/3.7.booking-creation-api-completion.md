# Story 3.7: Booking Creation API Completion

**Status:** Done

**Epic:** 3 - Room Exploration & Booking

**Issue Type:** Story

---

## Story

**As an** attendee,  
**I want** `unconf rooms` and `unconf book <room>` to complete booking creation against the live API,  
**so that** the core MVP booking journey works end-to-end instead of stopping at the client boundary.

---

## Acceptance Criteria

1. `POST /bookings` is registered in the live router and backed by a booking-creation handler
2. Booking wizard and direct CLI booking both succeed against a running server
3. Successful booking immediately updates `GET /bookings`, `unconf status`, room availability, and attendee-facing projections
4. Existing conflict and not-found behavior (`room_full`, `already_booked`, invalid room) remains intact end-to-end

---

## Tasks / Subtasks

- [x] **Task 1: Expose booking creation in the protected live router and document the active endpoint surface** (AC: 1, 2)
  - [x] Register `POST /bookings` in `internal/api/routes.go` alongside the existing authenticated booking lifecycle endpoints.
  - [x] Keep router wiring aligned with the existing handler injection path so no new server composition pattern is introduced.
  - [x] Update `docs/architecture/source-tree.md` current-layout notes to reflect the live attendee booking lifecycle endpoints.
  - **Reference:** [docs/prd/epic-3-room-exploration-booking.md#story-37-booking-creation-api-completion](../prd/epic-3-room-exploration-booking.md#story-37-booking-creation-api-completion), [docs/architecture/source-tree.md](../architecture/source-tree.md), [docs/architecture/10-cli-backend-architecture.md#103-api-server-architecture](../architecture/10-cli-backend-architecture.md#103-api-server-architecture)

- [x] **Task 2: Add booking-creation handler request validation and normalized HTTP error mapping** (AC: 1, 4)
  - [x] Implement the `POST /bookings` request contract in `internal/api/handlers/bookings.go` with `room_id`, `conference_id`, `privacy_setting`, and optional `notes`.
  - [x] Enforce authenticated-user presence, JSON request validation, and positive identifier checks before calling the service layer.
  - [x] Map service/domain errors to stable API responses for `not_found`, `room_full`, `already_booked`, `invalid_request`, and fallback `internal_error` behavior.
  - [x] Add or extend handler tests in `internal/api/handlers/bookings_test.go` for success, invalid request payloads, and create-path error mapping.
  - **Reference:** [docs/architecture/17-error-handling-strategy.md#171-error-response-format](../architecture/17-error-handling-strategy.md#171-error-response-format), [docs/architecture/coding-standards.md#domain-errors](../architecture/coding-standards.md#domain-errors), [docs/architecture/coding-standards.md#handler-error-mapping](../architecture/coding-standards.md#handler-error-mapping)

- [x] **Task 3: Complete booking service creation rules and booking projection assembly** (AC: 2, 3, 4)
  - [x] Implement or finalize `CreateBooking` in `internal/service/booking_service.go` using repository-layer lookups and existing dependency injection boundaries.
  - [x] Normalize and validate `privacy_setting`, reject invalid values, trim notes, validate room ownership against `conference_id`, prevent duplicate active bookings, and enforce room-capacity constraints.
  - [x] Preserve typed service errors in `internal/service/errors.go` so handler and client paths continue to rely on `errors.Is`-based mapping.
  - [x] Return the created booking using the existing response projection shape, including conference summary, room summary, occupancy counts, and privacy-aware roommate display so downstream reads update immediately.
  - [x] Keep hotel notification behavior best-effort and non-blocking if notifier delivery fails.
  - **Reference:** [docs/architecture/2-high-level-architecture.md#25-architectural-patterns](../architecture/2-high-level-architecture.md#25-architectural-patterns), [docs/architecture/4-data-models.md#44-room](../architecture/4-data-models.md#44-room), [docs/architecture/4-data-models.md#45-booking](../architecture/4-data-models.md#45-booking), [docs/architecture/coding-standards.md#critical-rules](../architecture/coding-standards.md#critical-rules)

- [x] **Task 4: Verify the live router now drives downstream attendee-facing projections without client rewrites** (AC: 2, 3, 4)
  - [x] Add or extend live-router integration coverage in `internal/api/routes_test.go` proving that successful `POST /bookings` creates a booking and immediately changes `GET /bookings` output.
  - [x] Verify the same flow updates `GET /conferences/:slug/rooms` availability counts (`spots_taken`, `spots_available`) for room explorer consumers.
  - [x] Cover end-to-end conflict and not-found cases through the live router for room-full, already-booked, and invalid-room scenarios.
  - [x] Use this story to close the backend gap for existing booking clients from Stories 3.4, 3.5, and 3.6 rather than introducing new CLI or TUI behavior in this scope.
  - **Reference:** [docs/stories/3.4.booking-wizard-tui-flow.md](3.4.booking-wizard-tui-flow.md), [docs/stories/3.5.direct-cli-booking.md](3.5.direct-cli-booking.md), [docs/stories/3.6.booking-status-command.md](3.6.booking-status-command.md), [docs/architecture/8-core-workflows.md#82-room-booking-flow-tui](../architecture/8-core-workflows.md#82-room-booking-flow-tui), [docs/architecture/source-tree.md](../architecture/source-tree.md)

- [x] **Task 5: Add targeted automated coverage and execute booking-API completion quality gates** (AC: 1-4)
  - [x] Extend `internal/service/booking_service_test.go` for create success, invalid privacy, already-booked, room-full, repository-conflict mapping, and cross-conference invalid-room paths.
  - [x] Extend `internal/api/handlers/bookings_test.go` and `internal/api/routes_test.go` so booking creation is covered at both unit and live-router integration levels.
  - [x] Run targeted verification for booking creation paths plus repository quality gates (`make test`, `make lint`) before story reconciliation.
  - [x] Treat full CLI lifecycle smoke automation as Story 3.8 scope; this story still requires enough coverage to prove the backend gap is closed.
  - **Reference:** [docs/architecture/15-testing-strategy.md#151-testing-pyramid](../architecture/15-testing-strategy.md#151-testing-pyramid), [docs/architecture/15-testing-strategy.md#152-current-coverage-notes-2026-04-15](../architecture/15-testing-strategy.md#152-current-coverage-notes-2026-04-15), [docs/architecture/commands.md](../architecture/commands.md)

---

## Dev Notes

### Story Source

- Story statement and acceptance criteria sourced from Epic 3 Story 3.7.
- [Source: docs/prd/epic-3-room-exploration-booking.md#story-37-booking-creation-api-completion]

### Previous Story Insights

- Story 3.4 implemented the booking wizard flow and Story 3.5 implemented direct CLI booking, but both stories noted that live booking completion still depended on `POST /bookings` being exposed in the API router.
- Story 3.6 relies on the existing booking projection shape from `GET /bookings`, so this story must preserve compatibility with status and room-availability consumers rather than redesigning the payload.
- [Source: docs/stories/3.4.booking-wizard-tui-flow.md]
- [Source: docs/stories/3.5.direct-cli-booking.md]
- [Source: docs/stories/3.6.booking-status-command.md]

### Architecture and Gap Context

- The intended booking path remains CLI/TUI client -> HTTP API -> handler -> service -> repository. This story completes the previously documented server-side router gap in that chain.
- `docs/architecture/10-cli-backend-architecture.md` and `docs/architecture/15-testing-strategy.md` still describe `POST /bookings` as missing; use `internal/api/routes.go` and `docs/architecture/source-tree.md` as the current runtime truth during reconciliation.
- The core TUI workflow already expects `POST /bookings` after room selection, so this story should complete the backend surface without introducing new UX patterns.
- [Source: docs/architecture/2-high-level-architecture.md#25-architectural-patterns]
- [Source: docs/architecture/8-core-workflows.md#82-room-booking-flow-tui]
- [Source: docs/architecture/10-cli-backend-architecture.md#101-cli-command-organization]
- [Source: docs/architecture/15-testing-strategy.md#152-current-coverage-notes-2026-04-15]
- [Source: docs/architecture/source-tree.md]
- [Source: internal/api/routes.go]

### Data and Domain Notes

- Booking creation requires valid `room_id`, `conference_id`, `privacy_setting`, and optional `notes`; the booking response should continue to expose status, room summary, conference summary, and roommate projection data.
- Relevant service-domain errors for this story are `ErrRoomNotFound`, `ErrConferenceNotFound`, `ErrRoomFull`, `ErrAlreadyBooked`, and `ErrInvalidPrivacySetting`.
- Room availability and attendee-facing projections depend on the same booking data being immediately visible through list and room-read paths after creation.
- [Source: docs/architecture/4-data-models.md#44-room]
- [Source: docs/architecture/4-data-models.md#45-booking]
- [Source: docs/architecture/17-error-handling-strategy.md#172-domain-errors]
- [Source: internal/service/errors.go]

### File Locations

- Router exposure: `internal/api/routes.go`
- Booking create handler and tests: `internal/api/handlers/bookings.go`, `internal/api/handlers/bookings_test.go`
- Booking service/domain rules and tests: `internal/service/booking_service.go`, `internal/service/booking_service_test.go`, `internal/service/errors.go`
- Live-router integration coverage: `internal/api/routes_test.go`
- Documentation alignment: `docs/architecture/source-tree.md`
- [Source: docs/architecture/source-tree.md]

### Technical Constraints

- Preserve repository-pattern boundaries: handlers call services, services call repositories, and CLI/TUI code remains API-client only.
- Follow existing standards for context propagation, structured logging with `log/slog`, wrapped errors, and dependency injection.
- Keep public behavior additive and minimal: expose the missing route, preserve existing domain/error semantics, and avoid unrelated client changes.
- [Source: docs/architecture/coding-standards.md#critical-rules]
- [Source: docs/architecture/coding-standards.md#error-wrapping]
- [Source: docs/architecture/coding-standards.md#context-propagation]

### Project Structure Notes

- `docs/architecture/source-tree.md` already reflects the booking lifecycle endpoints as live and should be treated as the authoritative architecture note for this story's completion state.
- The stale architecture/testing notes about `POST /bookings` being absent are useful historical context, but they should not override the implemented router, handler, service, or integration test behavior during Phase 3 reconciliation.

---

## Testing

### Testing Guidance

- Follow the project testing pyramid: service and handler unit tests plus live-router integration coverage using the existing in-memory SQLite integration harness.
- Use this story to prove the backend gap is closed; leave broader booking lifecycle smoke automation to Story 3.8.
- Complete at least one manual running-server verification for the existing direct-booking or wizard path so AC 2 is validated against the live server surface, not only isolated tests.
- [Source: docs/architecture/15-testing-strategy.md#151-testing-pyramid]
- [Source: docs/architecture/15-testing-strategy.md#154-manual-smoke-tests]
- [Source: docs/architecture/commands.md]

### Suggested Test Locations

- `internal/service/booking_service_test.go`
- `internal/api/handlers/bookings_test.go`
- `internal/api/routes_test.go`
- `cmd/server/main_integration_test.go`

### Key Test Scenarios

1. Authenticated `POST /bookings` returns `201 Created` with booking status, room summary, conference summary, and normalized notes.
2. After successful creation, `GET /bookings` returns the new booking for the authenticated attendee.
3. After successful creation, `GET /conferences/:slug/rooms` updates room availability counts immediately.
4. Submitting a booking to a full room returns `409 room_full`.
5. Submitting a second active booking for the same user and conference returns `409 already_booked`.
6. Missing or cross-conference-invalid room input returns `404 not_found`.
7. Invalid privacy or malformed request body returns `400 invalid_request`.
8. Existing `unconf rooms` wizard or `unconf book <room>` flow succeeds against a running server now that `POST /bookings` is live.

---

## Story Draft Checklist Validation

### Validation Summary

- Story readiness: READY
- Clarity score: 9/10
- Major gaps: None blocking. Architecture-doc drift around the former router gap is noted explicitly so Phase 3 should reconcile against code and current source-tree notes, not older planned-state text.

| Category | Status | Issues |
|----------|--------|--------|
| 1. Goal & Context Clarity | PASS | None |
| 2. Technical Implementation Guidance | PASS | None |
| 3. Reference Effectiveness | PASS | None |
| 4. Self-Containment Assessment | PASS | None |
| 5. Testing Guidance | PASS | None |

### Developer Perspective

- Implementable as written: Yes.
- Likely questions: whether to add more end-to-end CLI smoke automation now or keep that strictly in Story 3.8.
- Delay/rework risk: Low.

---

## Change Log

| Date       | Version | Description | Author |
|-----------|---------|-------------|--------|
| 2026-04-15 | 1.0     | Initial draft for Story 3.7 (Booking Creation API Completion) with AC-mapped tasks aligned to the implemented router, handler, service, and test work | Bob (Scrum Master) |
| 2026-04-15 | 1.1     | Story draft checklist validation passed; status set to Approved | GitHub Copilot (Scrum Master) |
| 2026-04-15 | 1.2     | Reconciled Phase 3 implementation, added cross-conference invalid-room coverage, ran targeted booking-path validations plus `make test` and `make lint`, and set status to Ready for Review | GitHub Copilot (Dev) |
| 2026-04-15 | 1.3     | Added server-backed direct booking CLI and wizard submission coverage plus stronger post-create attendee projection assertions, then re-ran repository quality gates | GitHub Copilot (Dev) |
| 2026-04-15 | 1.4     | Closed the QA-driven AC2 evidence gap by converting the direct CLI and wizard running-server tests to the real in-memory app router stack, recorded the review-fix validation pass, and noted the remaining accepted concurrency risk | GitHub Copilot (Dev) |
| 2026-04-15 | 1.5     | Recorded the PASS QA re-review artifacts and advanced story status to QA Passed while retaining the accepted concurrent capacity race risk | GitHub Copilot (Dev) |
| 2026-04-15 | 1.6     | Phase 6 Step 1 reconciliation completed: reviewed closure sections, confirmed PASS QA gate coherence, and set story status to Done for closure | Bob (Scrum Master) |

---

## Dev Agent Record

*To be completed by the development agent during implementation and finalization.*

### Agent Model Used

- GPT-5.4 (CLI)

### Debug Log References

- `gofmt -w internal/service/booking_service_test.go internal/api/routes_test.go`
- Targeted booking-path validation across `internal/service`, `internal/api/handlers`, `internal/api`, `internal/client`, `internal/cli`, and `internal/tui/wizard`
- `go test ./internal/cli ./internal/tui/wizard ./internal/service ./internal/api ./internal/client`
- `make test`
- `make lint`

### Completion Notes List

- Reconciled the pre-existing booking creation implementation instead of re-implementing it; the only Phase 3 code gap found was missing automated coverage for cross-conference invalid-room rejection.
- Closed QA finding `TEST-3.7-001` by replacing the ad hoc `httptest` handlers in the direct booking CLI and wizard running-server tests with a shared in-memory SQLite plus real router/handler harness.
- Verified the review-fix path with `go test ./internal/cli ./internal/tui/wizard ./internal/service ./internal/api ./internal/client`, then confirmed full repository validation still passes with `make test` and `make lint`.
- Preserved the existing client behavior from Stories 3.4, 3.5, and 3.6; this story closes the live API gap without adding new booking UX.
- Accepted follow-up risk remains on concurrent last-spot booking: room-capacity enforcement is still a read-then-insert path and should move to a transactional repository flow in a later story instead of broadening this review-fix round.
- Phase 6 Step 1 closure reconcile complete: task checklist, completion notes, file list, and QA PASS record were reviewed for consistency; no further implementation changes were required before marking the story Done.

### File List

- `docs/architecture/source-tree.md`
- `docs/qa/gates/3.7-booking-creation-api-completion.yml`
- `docs/stories/3.7.booking-creation-api-completion.md`
- `internal/api/handlers/bookings.go`
- `internal/api/handlers/bookings_test.go`
- `internal/api/routes.go`
- `internal/api/routes_test.go`
- `internal/cli/book_test.go`
- `internal/service/booking_service.go`
- `internal/service/booking_service_test.go`
- `internal/service/errors.go`
- `internal/tui/wizard/model_test.go`

## QA Results

### Review Date: 2026-04-15
### Reviewer: Quinn (Test Architect)
### Gate Recommendation: CONCERNS

### Summary
- Re-reviewed the latest Story 3.7 commits across the handler, router, service, and booking-path test changes.
- No high-confidence functional defect was found in the live API booking-create path; AC1, AC3, and AC4 are covered through service, handler, and live-router tests.
- AC2 is only partially evidenced in-repo: the new direct CLI and wizard "running server" tests exercise the HTTP client path against ad hoc stub servers rather than the real router/server wiring.
- Reviewed reported validation evidence from the story record: booking-focused package validation, `go test ./internal/cli ./internal/tui/wizard ./internal/api`, `make test`, and `make lint`.

### Acceptance Criteria Trace
- **AC1** `POST /bookings` is registered in the live router and backed by a handler: **Covered** (`internal/api/routes.go`, `internal/api/handlers/bookings.go`, `internal/api/routes_test.go`)
- **AC2** Booking wizard and direct CLI booking succeed against a running server: **Partial** (`internal/cli/book_test.go` and `internal/tui/wizard/model_test.go` cover the real HTTP client path, but not the production server/router stack)
- **AC3** Successful booking immediately updates bookings, room availability, and attendee-facing projections: **Covered** (`internal/service/booking_service.go`, `internal/api/routes_test.go`)
- **AC4** Existing room-full, already-booked, and invalid-room behavior remains intact end-to-end: **Covered** (`internal/api/handlers/bookings_test.go`, `internal/api/routes_test.go`, `internal/service/booking_service_test.go`)

### Gate Status
- CONCERNS → `docs/qa/gates/3.7-booking-creation-api-completion.yml`
- Finalization check: add one real running-server CLI or wizard smoke test, or attach manual verification evidence, before treating AC2 as fully closed.

### Review Date: 2026-04-15
### Reviewer: Quinn (Test Architect)
### Gate Recommendation: CONCERNS

### Summary
- Phase 5 re-review found no blocking serial-path defect in the live booking-create flow; the focused suites still pass, including `go test ./internal/service ./internal/api ./internal/cli ./internal/tui/wizard ./internal/client`.
- AC2 remains only partially evidenced: the direct CLI and wizard "running server" tests in `internal/cli/book_test.go` and `internal/tui/wizard/model_test.go` exercise authenticated HTTP calls against ad hoc `httptest` handlers, while the story testing guidance still requires one real running-server verification.
- A second medium-severity reliability concern remains on the live `POST /bookings` path: room capacity is enforced with a read-then-insert sequence in `internal/service/booking_service.go`, so concurrent last-spot requests can still overbook a room.

### Acceptance Criteria Trace
- **AC1** `POST /bookings` is registered in the live router and backed by a handler: **Covered** (`internal/api/routes.go`, `internal/api/handlers/bookings.go`, `internal/api/routes_test.go`)
- **AC2** Booking wizard and direct CLI booking succeed against a running server: **Partial** (`internal/cli/book_test.go` and `internal/tui/wizard/model_test.go` use stub servers, and the story still asks for one real running-server verification)
- **AC3** Successful booking immediately updates bookings, room availability, and attendee-facing projections: **Covered** (`internal/api/routes_test.go`, `internal/service/booking_service.go`)
- **AC4** Existing room-full, already-booked, and invalid-room behavior remains intact end-to-end: **Covered for serial flows** (`internal/api/handlers/bookings_test.go`, `internal/api/routes_test.go`, `internal/service/booking_service_test.go`); **concurrent capacity enforcement still needs follow-up**

### Gate Status
- CONCERNS → `docs/qa/gates/3.7-booking-creation-api-completion.yml`
- Finalization checks:
  - add one real running-server CLI or wizard smoke path, or attach manual verification evidence, to fully close AC2
  - follow up with transactional room-capacity enforcement plus a concurrent last-spot regression test before treating the booking path as reliability-complete

### Review Date: 2026-04-15
### Reviewer: Quinn (Test Architect)
### Gate Recommendation: PASS

### Summary
- Affected-area re-review confirms the latest fix closes the prior AC2 evidence gap: both `TestBookCmd_CreatesBookingAgainstRunningServer` and `TestModel_SubmitsBookingAgainstRunningServer` now run against an in-memory SQLite-backed application router/server harness instead of ad hoc stub handlers.
- Re-ran the two running-server attendee-path tests directly; both passed. Combined with the already-reported `go test ./internal/cli ./internal/tui/wizard ./internal/service ./internal/api ./internal/client`, `make test`, and `make lint` results, the acceptance-criteria evidence is sufficient for this story.
- The only remaining concern in scope is the already-known architectural race on concurrent last-spot booking. That reliability risk predates this fix round, was not introduced by the reviewed changes, and does not block QA pass for Story 3.7.

### Acceptance Criteria Trace
- **AC1** `POST /bookings` is registered in the live router and backed by a handler: **Covered** (`internal/api/routes.go`, `internal/api/handlers/bookings.go`, `internal/api/routes_test.go`)
- **AC2** Booking wizard and direct CLI booking succeed against a running server: **Covered** (`internal/cli/book_test.go`, `internal/tui/wizard/model_test.go`)
- **AC3** Successful booking immediately updates bookings, room availability, and attendee-facing projections: **Covered** (`internal/api/routes_test.go`, `internal/cli/book_test.go`, `internal/service/booking_service.go`)
- **AC4** Existing room-full, already-booked, and invalid-room behavior remains intact end-to-end: **Covered for the accepted serial path** (`internal/api/handlers/bookings_test.go`, `internal/api/routes_test.go`, `internal/service/booking_service_test.go`)

### Gate Status
- PASS → `docs/qa/gates/3.7-booking-creation-api-completion.yml`
- Accepted risk retained for follow-up: concurrent room-capacity enforcement is still non-atomic and should be addressed in a later transactional repository change.
