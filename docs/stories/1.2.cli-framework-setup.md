# Story 1.2: CLI Framework Setup

**Status:** Ready for Review

**Epic:** 1 - Foundation & Authentication

---

## Story

**As a** developer,  
**I want** the Cobra CLI framework initialized with a root command,  
**so that** I can add subcommands for UNCONF features.

---

## Acceptance Criteria

1. Cobra initialized with `unconf` as root command
2. Viper integrated for configuration management
3. `--help` flag works and shows welcoming help text
4. `--version` flag displays version information
5. Configuration file support (`.unconf.yaml` or similar)
6. Basic error handling and exit codes established

---

## Dev Notes

### Technology Stack Reference

[Source: tech-stack.md#core-technologies]

This story uses the following framework versions:
- **Cobra:** v1.10+ (Command structure framework)
- **Viper:** v1.21+ (Configuration management)
- **Logging:** log/slog (Go 1.24+ stdlib, structured logging)

### Project Structure & File Locations

[Source: 11-unified-project-structure.md]

New CLI code must be created in these locations:

```
internal/cli/
├── root.go           # Root command, global flags
└── ...               # Other commands will be added in future stories

cmd/unconf/
└── main.go           # CLI entry point (already created in Story 1.1)
```

The CLI entry point in `cmd/unconf/main.go` should:
- Import `internal/cli` package
- Call `NewRootCmd()` from root.go
- Execute the root command and handle exit codes

### Cobra Root Command Structure

[Source: 10-cli-backend-architecture.md#101-cli-command-organization]

The CLI is organized as:
```
internal/cli/
├── root.go           # Root command, global flags
├── login.go          # unconf login
├── logout.go         # unconf logout
├── list.go           # unconf list
├── info.go           # unconf info <slug>
├── checkout.go       # unconf checkout <slug>
├── config.go         # unconf config
├── rooms.go          # unconf rooms (TUI)
├── book.go           # unconf book <room>
├── status.go         # unconf status
├── cancel.go         # unconf cancel
├── invite.go         # unconf invite <username>
├── requests.go       # unconf requests
├── attendees.go      # unconf attendees
├── dashboard.go      # unconf dashboard (organizer)
├── create.go         # unconf create (organizer)
├── confirm.go        # unconf confirm (organizer)
└── export.go         # unconf export (organizer)
```

For this story, only the root command is implemented. Future stories will add individual subcommands.

### Naming Conventions

[Source: coding-standards.md#naming-conventions]

- **Files:** snake_case (e.g., `root.go`, `booking_service.go`)
- **Packages:** lowercase, single word (e.g., `cli`, `service`)
- **Public functions/structs:** PascalCase (e.g., `NewRootCmd()`, `RootCmd`)
- **Private functions/methods:** camelCase (e.g., `handleVersion()`)
- **Variables:** camelCase (e.g., `versionFlag`, `configFile`)

### Configuration Management (Viper)

[Source: coding-standards.md#section-5-configuration]

Key principles for configuration:
- NEVER read environment variables directly (`os.Getenv`)
- Always use a Config wrapper that abstracts Viper
- Configuration should support:
  - Command-line flags (--flag-name)
  - Configuration file (.unconf.yaml)
  - Environment variables (UNCONF_FLAG_NAME format)
  - Sensible defaults

For this story, establish the config loading pattern. Future stories will expand the config structure.

### Logging Standards

[Source: coding-standards.md#section-4-logging]

- ONLY use `log/slog` for all logging output
- NEVER use `fmt.Println()`, `log.Printf()`, or other logging methods in production code
- Use structured logging with key-value pairs:
  ```go
  slog.Info("CLI started",
      "version", version,
      "config_file", configFile,
  )
  ```

### Dependencies & Error Handling

[Source: coding-standards.md#section-2-error-wrapping]

Error handling pattern:
- Always wrap errors with context using `fmt.Errorf("description: %w", err)`
- Never return raw errors without context
- CLI should exit with appropriate exit codes:
  - `0` = success
  - `1` = general error
  - `2` = usage error

### Go Version & Module

[Source: tech-stack.md#core-technologies]

- **Go:** 1.24+ required
- Root `go.mod` is already initialized from Story 1.1
- Add dependencies:
  ```go
  require (
      github.com/spf13/cobra v1.10.2
      github.com/spf13/viper v1.21.0
  )
  ```

### Previous Story Context

Story 1.1 (Project Scaffolding) established:
- Go module initialized with workspace path
- Basic project structure in place
- `.gitignore` configured
- Makefile with build targets
- `golangci-lint` configuration

This story builds on that foundation by adding the CLI framework layer.

---

## Tasks / Subtasks

### Task 1: Set up internal/cli package structure (AC: 1)
- [x] Create `internal/cli/` directory
- [x] Create `internal/cli/root.go` with root command function
  - [x] Define `NewRootCmd()` function that returns a `*cobra.Command`
  - [x] Set root command name to "unconf"
  - [x] Set root command short description (welcoming message)
  - [x] Set root command long description with usage information

### Task 2: Implement Cobra root command with help and flags (AC: 1, 3, 4)
- [x] Import `"github.com/spf13/cobra"` in root.go
- [x] Create `--version` flag that displays version information
  - [x] Version should come from a package variable or config (e.g., `Version = "v1.0.0-alpha"`)
  - [x] Flag should use `cobra.Command.Version` field
- [x] Create `--help` flag (built-in to Cobra, verify it works)
  - [x] Ensure help text is welcoming and guides users to next steps
  - [x] Example: "UNCONF CLI — Unconference registration made easy"

### Task 3: Integrate Viper for configuration management (AC: 5, 6)
- [x] Create `internal/config/` package with config wrapper
  - [x] Do NOT call `os.Getenv()` directly anywhere
  - [x] Create `Config` type with methods like `GetAPIEndpoint()`, `GetConfigFile()`, etc.
  - [x] Implement `LoadConfig()` function that:
    - [x] Reads from environment variables with `UNCONF_` prefix
    - [x] Reads from `.unconf.yaml` config file (if exists)
    - [x] Merges CLI flags with config file and env vars
    - [x] Sets sensible defaults for required values
- [x] Update root command to initialize config before running

### Task 4: Add configuration file support (AC: 5)
- [x] Create sample `.unconf.yaml` configuration file in project root
  - [x] Include example settings: `api_endpoint`, `log_level`, `config_file` path
  - [x] Add comments explaining each setting
  - [x] Document location: `.unconf.yaml` in project root
- [x] Support config file specified via `--config` flag
- [x] Support config file auto-discovery in home directory (e.g., `~/.unconf.yaml`)
- [x] Viper should bind flags to config keys

### Task 5: Establish basic error handling and exit codes (AC: 6)
- [x] Define error handling pattern for CLI
  - [x] Create `internal/cli/errors.go` or similar
  - [x] Define exit codes: `Success = 0`, `GeneralError = 1`, `UsageError = 2`
  - [x] Implement error wrapping with `fmt.Errorf("description: %w", err)` in all functions
- [x] Root command's `RunE` function should:
  - [x] Return errors with proper context
  - [x] CLI should use Cobra's error handling to exit with appropriate codes

### Task 6: Set up CLI entry point (AC: 1)
- [x] Update `cmd/unconf/main.go` (created in Story 1.1) to:
  - [x] Import `"github.com/unconf-vibes/unconf/internal/cli"`
  - [x] Create root command using `cli.NewRootCmd()`
  - [x] Execute the command: `rootCmd.Execute()`
  - [x] Handle execution errors with proper exit codes

### Task 7: Add structured logging to CLI startup (AC: 1, 6)
- [x] Import `"log/slog"` in root.go
- [x] Add info-level log at startup showing:
  - [x] CLI version
  - [x] Config file location (if loaded)
  - [x] Command being executed
- [x] Example: `slog.Info("UNCONF CLI starting", "version", Version, "config_file", configPath)`

### Task 8: Unit tests for root command (Testing)
- [x] Create `internal/cli/root_test.go`
  - [x] Test case: `NewRootCmd()` returns non-nil command
  - [x] Test case: `--help` flag works and outputs help text
  - [x] Test case: `--version` flag outputs version string
  - [x] Test case: Invalid flags produce usage error (exit code 2)
  - [x] Use Go testing stdlib (`testing.T`)
  - [x] Use `testify/assert` for assertions (included in tech-stack.md)

### Task 9: Integration test for config loading (Testing)
- [x] Create `internal/config/config_test.go`
  - [x] Test case: Config loads from `.unconf.yaml` file
  - [x] Test case: Config loads from environment variables (UNCONF_ prefix)
  - [x] Test case: CLI flags override config file and env vars
  - [x] Test case: Defaults applied when values not provided
  - [x] Use in-memory or temporary config files for testing

### Task 10: Update go.mod with Cobra and Viper dependencies
- [x] Run `go get github.com/spf13/cobra@v1.10.2`
- [x] Run `go get github.com/spf13/viper@v1.21.0`
- [x] Run `go mod tidy` to clean up
- [x] Verify versions pinned in go.mod match tech-stack.md requirements

---

## Testing

### Test File Locations & Patterns

[Source: coding-standards.md#naming-conventions]

- Unit tests: `{package}/{file}_test.go` (e.g., `internal/cli/root_test.go`)
- Integration tests: Same location with `*_integration_test.go` suffix (if needed)
- Use Go's `testing.T` for all tests
- Use `testify/assert` for assertions

### Testing Pyramid & Strategy

[Source: 15-testing-strategy.md#151-testing-pyramid]

- **60% Unit Tests:** Root command setup, flag parsing, config loading
- **30% Integration Tests:** Config file loading + Viper integration (use temporary test files)
- **10% E2E:** Manual testing of actual CLI execution

### Test Examples to Follow

[Source: 15-testing-strategy.md#152-test-examples]

```go
// Unit test pattern
func TestNewRootCmd(t *testing.T) {
    cmd := NewRootCmd()
    assert.NotNil(t, cmd)
    assert.Equal(t, "unconf", cmd.Use)
}

// Integration test pattern
func TestConfigLoading(t *testing.T) {
    // Create temporary config file
    // Load config
    // Assert values loaded correctly
}
```

### Testing Requirements for This Story

- Minimum coverage for root.go: 80%
- All public functions must have tests
- Error paths must be tested
- Config loading must be integration-tested with actual YAML files
- Manual testing: Run `unconf --help`, `unconf --version` from terminal

---

## Change Log

| Date       | Version | Description | Author |
|-----------|---------|-------------|--------|
| 2026-02-14 | 1.0     | Initial draft - CLI Framework Setup | Scrum Master |
| 2026-02-14 | 1.1     | Implemented Cobra root command, Viper config wrapper, CLI entrypoint/exit codes, and tests | James (Dev Agent) |

---

## Dev Agent Record

*This section will be completed by the development agent during implementation.*

### Agent Model Used
GPT-5.3-Codex

### Debug Log References
- `go get github.com/spf13/cobra@v1.10.2 github.com/spf13/viper@v1.21.0 github.com/stretchr/testify@v1.11.1 && go mod tidy`
- `go test ./...`
- `golangci-lint run ./...`

### Completion Notes
- Implemented `internal/cli/root.go` with `unconf` root command, welcoming help text, `Version` support, `--config` flag, and startup `slog` logging.
- Added `internal/config/config.go` Viper wrapper with `UNCONF_` env support, `.unconf.yaml` discovery (cwd + home), explicit config file loading, defaults, and flag binding support.
- Added CLI exit code mapping in `internal/cli/errors.go` and wired `cmd/unconf/main.go` to execute CLI with proper non-zero exit behavior.
- Added sample `.unconf.yaml` in project root.
- Added test coverage for root command flags/help behavior and config loading precedence/defaults/home discovery.
- Verified all tests and lint pass.

### File List
- `.unconf.yaml`
- `cmd/unconf/main.go`
- `go.mod`
- `go.sum`
- `internal/cli/errors.go`
- `internal/cli/root.go`
- `internal/cli/root_test.go`
- `internal/config/config.go`
- `internal/config/config_test.go`

---

## QA Results

### Review Date: 2026-02-14

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation is solid and aligns with architecture and coding standards for this story scope. All acceptance criteria are functionally satisfied, test coverage is present for root command and config behavior, and automated tests/lint pass in current workspace.

### Refactoring Performed

No refactoring performed during QA review (advisory findings only).

### Compliance Check

- Coding Standards: ✓ No blocking coding standards violations for this story scope.
- Project Structure: ✓ Files are in expected `cmd/`, `internal/cli`, and `internal/config` locations.
- Testing Strategy: ✓ Unit/integration-oriented tests exist for CLI root command and config loading.
- All ACs Met: ✓ AC1-AC6 validated via implementation and passing tests.

### Improvements Checklist

- [x] Verified AC coverage against implementation and tests
- [x] Verified targeted and full test suite execution
- [x] No immediate fixes required for story acceptance

### Security Review

No security regressions identified in reviewed scope. No direct DB access from CLI and no prohibited direct env reads in CLI/config flow.

### Performance Considerations

No notable performance risks in current scope. Startup path remains lightweight.

### Files Modified During Review

- `docs/stories/1.2.cli-framework-setup.md` (QA Results section only)

### Gate Status

Gate: PASS → `docs/qa/gates/1.2-cli-framework-setup.yml`
Risk profile: Not generated separately in this review
NFR assessment: Not generated separately in this review

### Recommended Status

[✓ Ready for Done]
(Story owner decides final status)
